Update the logos-docs git submodule to fetch the latest documentation and specs.

Run: `git submodule update --init --remote docs/logos-docs`

After updating, report what changed (show the new commit hash) and briefly list any files that were added or modified in the submodule.
